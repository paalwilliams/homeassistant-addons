name: FMD
version: "0.14.2"
slug: fmd
boot: manual
description: Decentralised remote device location and control  || remove entry
url: "https://github.com/paalwilliams/homeassistant-addons"
arch:
  - amd64
  - aarch64
init: true
map:
  - addon_config:rw
  - data:rw          # Persistent storage
panel_icon: map
panel_title: FMD
ports:
  8080/tcp: 8080
webui: http://[HOST]:[PORT:8080]
legacy: true
ports_description:
  8080/tcp: Web interface port
